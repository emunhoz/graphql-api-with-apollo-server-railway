{
  "name": "graphql-api-with-apollo-server-railway",
  "version": "0.0.1",
  "description": "",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "dev:watch": "bun run --watch src/index.ts",
    "build": "tsup src",
    "build:bun": "bun build src/index.ts --target bun --format esm --outdir dist --minify",
    "start": "bun run build && bun run dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tsup": "^8.2.4",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "@apollo/server": "^4.7.3",
    "class-validator": "^0.14.1",
    "graphql": "^16.9.0",
    "reflect-metadata": "^0.2.2",
    "type-graphql": "^2.0.0-rc.2"
  }
}
